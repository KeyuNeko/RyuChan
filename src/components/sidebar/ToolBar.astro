---
import Card from "../temple/Card.astro";
import ScrollToTop from "../widgets/ScrollToTop.astro";
import ThemeToggle from "../widgets/ThemeToggle.astro";
import { Icon } from "astro-icon/components";
import { t } from "@config";
---

<Card>
  <div class="toolbar-container p-4">
    <div class="grid grid-cols-3 md:grid-cols-2 lg:grid-cols-3 justify-center items-center gap-4 flex-wrap">
      <!-- Navigation Buttons -->
      <a
        href="/blog#article-filter"
        class="btn btn-circle btn-md border-base-content/20 bg-base-100 shadow-sm hover:scale-110"
        aria-label="内容筛选"
        title="内容筛选"
      >
        <Icon name="lucide:sliders-horizontal" class="w-5 h-5" />
      </a>

      <a
        href="/blog/archives"
        class="btn btn-circle btn-md border-base-content/20 bg-base-100 shadow-sm hover:scale-110"
        aria-label={t("label.archivePage")}
        title={t("label.archivePage")}
      >
        <Icon name="lucide:archive" class="w-5 h-5" />
      </a>

      <ThemeToggle className="sidebar-theme" />

      <slot />
    </div>
  </div>
</Card>

<!-- ScrollToTop is placed outside the Card to be fixed at the bottom of the screen -->
<ScrollToTop />
